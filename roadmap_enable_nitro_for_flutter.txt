# GuelmaHistory — Enable Nuxt/Nitro for Flutter Roadmap

## Purpose

Track the Nuxt 4 and Nitro backend work required to support the GuelmaHistory Flutter admin application without breaking the existing Vuetify 4 web admin.

```text
Nuxt/Vuetify web admin ─┐
                        ├─ Nuxt/Nitro API ─ PostgreSQL/Drizzle
Flutter Android/iOS ────┘                  └─ Cloudinary
```

Flutter must never connect directly to PostgreSQL, Drizzle, Cloudinary administration credentials, or other private services.

## Tracking legend

```text
[ ] Not started
[~] In progress
[x] Completed and verified
[!] Blocked — add the reason beside the item
```

After completing a section, record:

```text
Completed:
Commit/MR:
Notes:
```

---

# MILESTONE 1 — Audit the Existing Nitro API

## Current system

The existing application already provides:

- Nuxt 4 and Nitro/H3 server routes
- PostgreSQL with Drizzle ORM
- Zod server validation
- `nuxt-auth-utils` cookie sessions
- Roles: `user < author < admin`
- Admin article, category, author, reported-comment, subscriber, and profile APIs
- Arabic and French article fields
- Cloudinary article media uploads

## Tasks

- [x] Inventory every `/api/admin/**` endpoint
- [x] Inventory administrator routes under `/api/auth/**`
- [x] Record request, response, query, error, and permission contracts
- [x] Identify handlers containing business logic that belongs in services
- [x] Document current pagination, search, filtering, and sorting
- [x] Document article media formats and upload limits
- [x] Document the article body format rendered by Nuxt
- [x] Confirm required Arabic and French fields
- [x] Confirm current publishing behavior based on `publishedAt`
- [x] Identify browser-only assumptions such as cookie authentication

## Verified audit findings

### Existing admin route groups

```text
/api/admin/articles
/api/admin/articles/:slug
/api/admin/articles/media/upload
/api/admin/authors
/api/admin/categories
/api/admin/comments/flags
/api/admin/profile
/api/admin/subscribers
/api/admin/upload
```

The administrator web login currently uses:

```text
POST /api/auth/login
POST /api/auth/logout
POST /api/auth/forgot-password
POST /api/auth/reset-password
```

Authentication is browser-oriented. Login creates a sealed `nuxt-auth-utils` cookie session with `setUserSession()`, protected routes read it with `getUserSession()`, and logout clears it with `clearUserSession()`.

### Authorization findings

Article write/list handlers and profile/report handlers inspected during the audit use `requireAdmin()`.

The audit originally found unguarded category, author, and subscriber management routes. Explicit `requireAdmin()` checks have now been added to the following handlers without changing their response contracts:

```text
GET    /api/admin/categories
GET    /api/admin/categories/count
POST   /api/admin/categories
PATCH  /api/admin/categories/:id
DELETE /api/admin/categories/:id
GET    /api/admin/authors
GET    /api/admin/authors/count
GET    /api/admin/authors/list
POST   /api/admin/authors
GET    /api/admin/subscribers
GET    /api/admin/subscribers/count
GET    /api/admin/subscribers/export
```

A second handler-by-handler pass added explicit `requireAdmin()` checks to:

```text
PATCH  /api/admin/authors/:id
DELETE /api/admin/authors/:id
GET    /api/admin/authors/by-slug/:slug
PATCH  /api/admin/authors/by-slug/:slug
DELETE /api/admin/authors/by-slug/:slug
PATCH  /api/admin/subscribers/:id/subscription
DELETE /api/admin/subscribers/:id
POST   /api/admin/upload
```

The legacy `/api/admin/upload` placeholder previously returned a misleading successful response without uploading anything. It is now protected and returns `501 Not Implemented`, directing article media clients to `/api/admin/articles/media/upload`.

A route being under `/api/admin` does not protect it automatically. All new admin handlers must include an explicit guard or use a future protected router/middleware mechanism with equivalent test coverage.

### Business-logic findings

Articles already have a dedicated `ArticleService` covering validation, relation checks, transactions, serialization, publishing side effects, media replacement, and Cloudinary cleanup.

Categories, authors, subscribers, profile operations, and reported-comment operations do not yet follow one consistent service structure. Several handlers or utilities access Drizzle directly. These domains require service extraction before versioned routes are added.

Current error responses are inconsistent:

- Article routes commonly pass errors through `toH3Error()`.
- Validation errors become one joined message without structured field errors.
- Some comment routes wrap results as `{ success, data }`.
- Other routes return arrays or domain objects directly.
- Unexpected production errors may currently expose `Error.message`.

### Pagination, filtering, and sorting

Article listing supports:

```text
page, limit, search, categoryId, category, authorId, tagId,
featured, status, sortBy, sortOrder
```

Article `limit` is capped at 100. Article status is inferred from `publishedAt`: non-null is `published`, null is `draft`.

The article response currently uses a `pagination` object with `currentPage`, `perPage`, `total`, `lastPage`, `hasNextPage`, and `hasPrevPage`. Other inspected admin lists return unpaginated arrays. The versioned API therefore needs a common pagination contract.

Article search currently applies only to `titleAr`, despite the bilingual model. This must be reviewed for Arabic/French mobile search.

### Article content and publishing

Creation currently requires both:

```text
titleAr, titleFr, bodyAr, bodyFr
```

Arabic and French excerpts are optional. Article gallery captions are also bilingual. Bodies are stored as strings, so Flutter must preserve the exact content format accepted and rendered by the Nuxt application.

There is no dedicated editorial status column. `publishedAt` is authoritative:

```text
publishedAt != null → published
publishedAt == null → draft
```

The first transition from null to a date triggers newsletter alerts. Mobile autosave must not set `publishedAt` accidentally.

### Media upload

`POST /api/admin/articles/media/upload`:

- Requires `admin`
- Accepts multipart field `file`
- Accepts declared `image/*` and `video/*`
- Has an endpoint limit of 100 MiB
- Has a Nuxt route-rule request limit of approximately 110 MB
- Uploads through server-only Cloudinary credentials
- Preserves an original image
- Generates WebP thumbnail, slider, and main variants with Sharp
- Returns image/video metadata including Cloudinary `publicId`

The current check trusts the multipart MIME declaration and does not implement idempotency. Those issues remain for Milestone 6.

Completed: 2026-08-03
Commit/MR: tracked in the `dev` branch and !1
Notes: Milestone 1 audit completed from the current repository. Runtime security fixes and service refactoring remain separate implementation work.

---

# MILESTONE 2 — Define Stable Mobile API Contracts

## Goal

Create predictable contracts for Flutter while retaining existing web routes.

Use a versioned namespace:

```text
/api/v1/admin/**
```

Existing Nuxt routes may remain during migration. Old and versioned handlers must call the same server services rather than duplicate business logic.

## Tasks

- [x] Define a standard success response
- [x] Define list pagination metadata
- [x] Define stable machine-readable error codes
- [x] Define field-level validation errors
- [x] Use ISO 8601 UTC timestamps
- [x] Define nullable-field behavior
- [~] Define safe sort fields and maximum page sizes — shared page-size cap is 100; domain sort allowlists remain
- [~] Add typed request and response DTOs — common contracts exist; domain DTO work remains
- [~] Add response serializers instead of returning Drizzle rows directly — mobile article serializer implemented; expand per domain
- [x] Create and maintain OpenAPI documentation — `/api/v1/openapi.json` documents health and every implemented v1 route
- [x] Define API compatibility and deprecation rules

Recommended list response:

```json
{
  "data": [],
  "meta": {
    "page": 1,
    "pageSize": 20,
    "total": 0,
    "hasNextPage": false
  }
}
```

Recommended error response:

```json
{
  "error": {
    "code": "ARTICLE_NOT_FOUND",
    "message": "Article not found.",
    "fields": null,
    "requestId": "safe-request-id"
  }
}
```

Completed:
Commit/MR: tracked in the `dev` branch and !1
Notes: Added additive response/error contracts, pagination helpers, stable error mapping, request IDs, private no-store headers, and a reusable versioned Nitro handler. `/api/v1/health` provides a non-sensitive contract probe. `/api/v1/openapi.json` publishes the implemented OpenAPI 3.1 contract. v1 allows additive compatible changes; breaking changes require a new major version and deprecated versions use response headers before sunset. Existing Nuxt endpoint responses remain unchanged.

---

# MILESTONE 3 — Refactor Shared Server Logic

## Goal

Ensure Nuxt web handlers and Flutter API handlers use the same domain rules.

Recommended flow:

```text
Nitro handler
  → authenticate and authorize
  → validate request with Zod
  → call domain service
  → serialize response
```

## Tasks

- [~] Keep Nitro handlers thin — article/category/author/subscriber/report/profile handlers now delegate; transport validation and legacy wrappers remain
- [x] Centralize API error conversion for versioned routes
- [~] Centralize pagination parsing — pure normalization exists; query-schema integration remains
- [~] Centralize authentication and role guards — existing admin handlers now explicitly use `requireAdmin()`; shared web/mobile transport work remains
- [x] Reuse article services — `ArticleRepository` owns all article Drizzle reads/writes, transactions, relation checks, and row mapping; `ArticleService` owns business rules and external side effects
- [x] Reuse category services — category handlers now delegate to `CategoryService` and `CategoryRepository`
- [x] Reuse author services — ID/slug routes now share `AuthorService`, `AuthorRepository`, validation, slug generation, and safe deletion
- [x] Reuse reported-comment services — admin report listing/grouping and resolution use `CommentReportService` and `CommentReportRepository`
- [x] Reuse subscriber services — list/count/export/status/delete handlers now use `SubscriberService` and `SubscriberRepository`
- [x] Reuse profile services — profile/password/email/avatar domain logic now uses `AdminProfileService` and `AdminProfileRepository`
- [x] Keep validators independent from Vue components
- [~] Add tests for important service rules — common API helpers and centralized admin validators are covered; repository integration tests remain

Completed:
Commit/MR: tracked in the `dev` branch and !1
Notes: Completed service/repository boundaries for articles, categories, authors, subscribers, reported comments, and admin profiles. Cookie-session refresh and avatar HTTP caching remain in handlers as transport concerns. Shared API helpers and centralized admin validators have unit coverage; database-backed repository integration tests remain. Existing Nuxt response shapes are preserved.

---

# MILESTONE 4 — Add Secure Mobile Authentication

## Goal

Keep sealed cookie sessions for the Nuxt web admin and add a dedicated mobile authentication mechanism for Flutter.

Recommended mobile model:

- Short-lived access token
- Rotating refresh token
- Hashed refresh tokens stored in PostgreSQL
- One revocable session per device
- Refresh-token reuse detection
- Logout current device
- Logout all devices

Never store raw refresh tokens in the database or logs.

## Mobile session schema

Design a table containing fields such as:

```text
id
userId
tokenHash
tokenFamilyId
deviceId
deviceName
platform
appVersion
createdAt
lastUsedAt
expiresAt
revokedAt
replacedByTokenId
```

## Tasks

- [x] Design the mobile-session schema
- [x] Add a safe Drizzle migration
- [x] Add indexes for token lookup, user sessions, device, family, and expiration
- [x] Store only cryptographic token hashes
- [x] Define access-token issuer, audience, expiry, and token type
- [x] Define refresh-token expiry and rotation — configurable 30-day expiry, opaque tokens, and database rotation are implemented
- [x] Rotate refresh tokens atomically
- [x] Detect refresh-token reuse
- [x] Revoke the affected token family after reuse
- [x] Revoke current-device sessions on logout
- [x] Support logout from all devices
- [x] Revoke mobile sessions after password change/reset
- [x] Add safe expired-session cleanup — login opportunistically removes sessions expired for more than seven days without requiring a pipeline
- [x] Decide and enforce the maximum number of active devices — configurable, default 5; oldest device is revoked when necessary
- [x] Keep signing keys in server-only runtime configuration
- [x] Never put authentication secrets in `runtimeConfig.public`

## Endpoints

```text
POST   /api/v1/admin/auth/login    [x]
POST   /api/v1/admin/auth/refresh  [x]
POST   /api/v1/admin/auth/logout          [x]
POST   /api/v1/admin/auth/logout-all      [x]
GET    /api/v1/admin/auth/me              [x]
GET    /api/v1/admin/auth/sessions        [x]
DELETE /api/v1/admin/auth/sessions/:id    [x]
```

## Authorization integration

- [x] Preserve cookie authentication for Nuxt web
- [x] Add bearer access-token authentication for Flutter
- [x] Reuse current timing-safe password verification
- [x] Restrict the initial mobile release to `admin`
- [x] Re-read the account/role from the database during refresh
- [x] Reject deleted, demoted, expired, revoked, or password-stale accounts/sessions
- [x] Keep mobile transport authentication separate from domain authorization
- [x] Never accept a refresh token as an API access token

## Tests

- [x] Valid login — manually verified against the local database on 2026-08-04
- [x] Invalid credentials — manually verified as a generic `401 Invalid credentials.` response without tokens or internal details
- [~] Unverified email — rejected by shared authentication; endpoint coverage remains
- [~] Wrong role — exact admin role required; endpoint coverage remains
- [~] Expired access token — strict expiry verification and unit coverage implemented; endpoint coverage remains
- [x] Successful refresh rotation — manually verified locally on 2026-08-04
- [~] Concurrent refresh requests — initial `200 + 500` race was corrected to `200 + 401`; replacement-family revocation was then fixed using a fresh transaction snapshot. Final local 401 check for the winner's replacement token remains
- [x] Reused refresh token — old token rejected and token family revoked
- [x] Revoked device session — revoked access and refresh tokens rejected
- [x] Logout-all — all tested device access and refresh tokens rejected
- [~] Password-change invalidation — access and refresh authentication reject mobile sessions created before passwordChangedAt. Password changes also revoke all mobile sessions immediately. ESLint, Vue-TSC, and existing mobile-auth token/validator tests pass. Dedicated password-change invalidation regression coverage remains.
- [N/A] Deleted/demoted admin through UI -> application has no admin deletion/demotion flow
- [x] Authentication rate limits — eight invalid attempts returned `401`; attempts 9–15 returned `429`

Completed: Core mobile authentication happy-path, invalid-credential handling, rate limiting, rotation, sequential reuse detection, current-session logout, administrative session revocation, and logout-all were manually verified locally on 2026-08-04.
Commit/MR: tracked in the `dev` branch and !1
Notes: Concurrent refresh now returns the safe `200 + 401` pair. A follow-up exposed that the losing transaction could not see the winner's replacement row, so family revocation now runs from a fresh transaction snapshot. The final check that the replacement token returns `401` remains manual. Added the `mobile_admin_sessions` Drizzle schema and migration `0012`. It stores only refresh-token hashes and supports token families, per-device sessions, rotation links, revocation, expiration, and indexed cleanup. Added server-only mobile authentication policy, short-lived HS256 access tokens with strict claim verification, opaque refresh-token generation, SHA-256 refresh-token hashing, and a default five-device limit. Refresh tokens now have an atomic, single-use repository rotation operation; concurrent reuse revokes the entire token family. Versioned Flutter login and refresh endpoints reuse timing-safe admin authentication, validate device metadata, re-check account authorization during refresh, and are strictly rate-limited. A dedicated bearer guard verifies strict access-token claims, active database sessions, current admin role, email verification, profile completion, and password-change freshness. The `/auth/me` endpoint exposes a protected mobile contract without affecting cookie sessions. Flutter admins can list safe device metadata, revoke another owned device session, log out the current device, or log out every device. Ownership checks prevent cross-account session revocation, and token hashes are never serialized. The configured active-device limit is enforced transactionally during login; a new login replaces the same device and revokes the oldest device when the limit is reached. Core token tests cover valid signatures, expiry, wrong keys, malformed/refresh-token rejection, and deterministic refresh-token hashing. OpenAPI now documents login, refresh, bearer security, profile, device-session management, and logout contracts. Mobile login additionally rejects incomplete profiles and opportunistically deletes sessions expired for more than seven days, avoiding pipelines and public cleanup endpoints. Validator tests cover supported device metadata, strict unknown-field rejection, platform allowlisting, and refresh-token bounds. Admin password changes revoke every mobile session. Password-reset token claiming, password replacement, `passwordChangedAt`, and mobile-session revocation now commit atomically, preventing token replay races and stale mobile access. A safe expired-session deletion operation is available for a future cleanup trigger. Existing Nuxt cookie authentication is unchanged.

---

# MILESTONE 5 — Build the Versioned Flutter Admin API

## Dashboard

Suggested endpoint:

```text
GET /api/v1/admin/dashboard
```

- [x] Return article count
- [x] Return category count
- [x] Return author count
- [x] Return subscriber count
- [x] Return recent articles
- [x] Return unresolved report count where available
- [x] Do not return fake analytics or review queues

## Articles

- [x] Paginated article list
- [x] Search and safe sorting
- [x] Published/unpublished filter using the current model
- [x] Get article by a stable identifier
- [x] Create article
- [x] Update article
- [x] Delete article
- [~] Update home position where authorized — requires an explicit domain rule before exposure
- [x] Validate Arabic and French titles, excerpts, and bodies
- [x] Validate category and author/byline
- [x] Preserve the difference between owner and display author
- [x] Serialize cover and media variants consistently

## Categories

- [x] List/search categories
- [x] Create category
- [x] Update category
- [x] Delete safely
- [x] Prevent or handle deletion while articles reference the category

## Authors

- [x] Paginated/searchable list
- [x] Lightweight selector list
- [x] Create author
- [x] Update author
- [x] Delete safely

## Reported comments

- [x] List unresolved flags/reports
- [x] Resolve reports using existing behavior
- [x] Keep reports separate from any future moderation workflow

## Subscribers

- [x] Paginated list
- [x] Search and status filtering
- [x] Update subscription state
- [x] Delete where policy permits
- [x] Export with an explicit permission check
- [x] Minimize personal data in list responses

## Profile

- [x] Get profile
- [x] Update display name
- [x] Update email safely
- [x] Change password
- [x] Upload/get avatar
- [x] Revoke other mobile sessions after password changes

Completed:
Commit/MR: tracked in the `dev` branch
Notes: Started Milestone 5 with bearer-protected dashboard and article reads. The dashboard aggregates existing domain services and returns only real metrics. Flutter article listing reuses bilingual search, status filtering, allowlisted sorting, and the 100-row limit; detail lookup uses a stable UUID. Create, update, and delete routes delegate to the shared article service, preserving bilingual validation, relation checks, owner-versus-byline semantics, publishing behavior, and Cloudinary cleanup. A dedicated serializer emits ISO timestamps, media variants, and the common pagination contract. `homePosition` remains blocked until its domain authorization rule is defined. Category CRUD now reuses the shared hierarchy-cycle and reference-safe deletion rules, supports simple bilingual search, and serializes timestamps consistently. Author management provides a paginated bilingual search view, lightweight selector list, validated create/update operations, and deletion protection while articles still reference the byline. Reported-comment routes list grouped unresolved reports and resolve only report records, preserving the existing separation from comment deletion or future moderation workflows. Subscriber management now provides pagination, email search, status filtering, validated status changes, deletion, and bearer-protected CSV export. The mobile serializer uses an explicit field allowlist and never exposes confirmation or unsubscribe token material. Profile routes support safe profile reads, display-name and password-verified email changes, password changes that revoke every mobile session, and normalized WebP avatar upload/download with synchronized 20 MiB limits. OpenAPI documents every implemented authentication, dashboard, article, category, author, report, subscriber, and profile route; detailed domain component schemas remain to be expanded.

---

# MILESTONE 6 — Make Uploads Flutter-Compatible

## Goal

Retain Cloudinary while making mobile uploads secure and retry-safe.

## Tasks

- [x] Document accepted MIME types in code: JPEG, PNG, WebP, GIF, MP4, QuickTime, and WebM
- [x] Keep Nitro request-size rules synchronized with endpoint limits — existing route allows ~110 MiB for a 100 MiB file cap
- [x] Verify supported file content signatures and reject declared MIME mismatches
- [x] Return stable Cloudinary public IDs as media IDs and references
- [x] Support upload-progress-friendly single multipart requests
- [x] Add persisted, per-admin idempotency keys for safe retries
- [x] Prevent duplicate uploads for completed and concurrent retries
- [x] Handle failed image variant/original Cloudinary operations
- [x] Clean up partially uploaded image assets safely
- [x] Ignore client filenames for storage IDs and generate server IDs
- [x] Preserve original images where archival requirements apply
- [x] Generate optimized variants through trusted server behavior
- [x] Never expose Cloudinary API secrets to Flutter
- [x] Prevent deletion of media currently referenced by an article
- [x] Apply bearer authorization, request limits, and upload-specific rate limits

## Tests

- [ ] Valid image
- [ ] Valid supported video
- [x] Invalid MIME/type mismatch — unit covered
- [~] Oversized upload — endpoint and route-rule enforcement implemented; endpoint coverage remains
- [ ] Unauthenticated upload
- [ ] Duplicate retry
- [ ] Cloudinary failure
- [~] Attempt to delete referenced media — 409 guard implemented; database integration coverage remains

Completed:
Commit/MR: tracked in the `dev` branch
Notes: Started upload hardening by replacing prefix-only multipart trust with byte-signature detection and exact MIME matching for an explicit image/video allowlist. Unsupported content returns 415 and oversized files return 413. Unit coverage includes supported signatures and mismatch rejection. The bearer-protected Flutter upload route supports one multipart request for progress reporting, returns stable Cloudinary IDs, preserves originals, generates trusted WebP variants, ignores client filenames for storage IDs, and cleans up image assets created before a later upload failure. Upload-specific request and rate limits are configured. Upload idempotency is persisted for 24 hours per administrator. Retries with the same file return the stored response, concurrent retries return 409, and key reuse for different bytes is rejected. Failed uploads release the claim after Cloudinary cleanup. A bearer-protected cleanup route can remove abandoned editor uploads, but returns 409 when the Cloudinary public ID is referenced by article media. Public IDs and resource types are strictly validated; validator coverage rejects URLs, traversal, and unknown fields. Database integration coverage remains.

---

# MILESTONE 7 — Protect Article Drafts and Concurrent Editing

## Goal

Prevent Nuxt and Flutter editors from silently overwriting each other.

## Tasks

- [x] Add an article revision/version field
- [x] Add a safe migration for existing articles
- [x] Return the revision in article responses
- [x] Require the expected revision during Flutter updates
- [x] Increment revision atomically after successful Flutter updates
- [x] Return `409 Conflict` for stale Flutter updates — repository transaction result is returned to the service
- [x] Add a dedicated autosave contract
- [x] Ensure autosave cannot publish accidentally
- [x] Add persisted per-admin idempotency for retry-sensitive autosaves
- [x] Record last save time and actor for Flutter, Nuxt admin, and Nuxt author updates — assigned server-side from the authenticated principal
- [ ] Decide whether full article revision history is required
- [x] Update the Nuxt web editor to use revision checks
- [x] Expose the same behavior to Flutter

## Tests

- [~] Normal update increments revision — repository implementation and request-schema coverage complete; database integration remains
- [ ] Two clients edit the same revision
- [~] Stale update returns 409 — conditional update and conflict mapping implemented; database integration remains
- [~] Retry does not duplicate effects — persisted claims protect completed and post-commit failure paths; database integration coverage remains
- [~] Autosave does not publish — restricted schema and atomic unpublished-row condition implemented; endpoint integration coverage remains
- [~] Arabic and French content survive save/recovery — validator unit coverage complete; database recovery coverage remains

Completed:
Commit/MR: tracked in the `dev` branch
Notes: Article revisions start at 1 and Flutter updates conditionally match `expectedRevision`, incrementing atomically or returning 409. The repository now correctly returns the transaction result to the service and serializes the revision. Revision-aware updates preserve shared media cleanup and first-publish newsletter behavior. A dedicated bearer-protected autosave route accepts only bilingual draft text, requires a revision, rejects unknown or empty payloads, and refuses published articles; it cannot alter publishing, ownership, relations, media, slugs, or home placement. Autosaves now require a persisted per-admin idempotency key: completed retries return the original response, concurrent retries conflict, and key reuse for a different article or payload is rejected. If response persistence fails after the article transaction commits, the in-flight claim is retained so the stale write cannot execute again; the client receives a safe reload instruction. Request hashing uses a canonical field order, so retries with identical validated values are recognized even when JSON property order differs. OpenAPI documents the restricted fields, bearer authentication, idempotency header, and conflict responses. Unit tests cover valid bilingual text, nullable excerpts, required positive integer revisions for normal updates and autosave, empty payloads, and rejection of publishing or structural autosave fields. Autosave and explicit Flutter updates record `lastSavedAt` and `lastSavedByUserId` atomically; both are server-owned and the actor comes only from the authenticated principal. The stable mobile serializer returns the save timestamp but omits the internal actor UUID. The Drizzle index declaration matches migration `0016`. Autosave now checks draft status in the same conditional database update as the revision, closing the race where another editor could publish between the initial read and write. The Nuxt admin and author editors now retain the loaded revision, send it on updates, adopt the returned revision, record their authenticated save actor, and display a clear conflict message. Both web PATCH handlers require a positive revision through the same centralized Zod schema used by Flutter and preserve their existing authorization. Database integration tests remain.

---

# MILESTONE 8 — Align the Nuxt Web Admin

## Goal

Keep the Vuetify 4 administration interface compatible with all backend changes.

## Tasks

- [x] Preserve cookie-based web authentication — the web admin still signs in through `/api/auth/login` and the sealed `nuxt-auth-utils` cookie; the central API middleware enforces `/api/admin/**` and `/api/author/**` through the same `requireRole()` cookie path, and Flutter bearer authentication remains a separate mechanism
- [x] Verify all admin middleware — `admin.ts` requires the exact `admin` role, `author.ts` uses `hasRole()` so admins inherit, `admin-guest.ts` and `author-guest.ts` redirect logged-in visitors away from the login pages by role without creating a loop, and `user-layout.global.ts` only swaps a layout and carries no authorization. Both login pages sanitize the `?redirect=` query by stripping the locale prefix, requiring the path to stay inside `/admin` or `/author`, and rejecting protocol-relative `//` values, so the guards cannot be turned into an open redirect
- [x] Use standardized API errors in web composables — `app/utils/apiError.ts` reads both the versioned `{ error: { code, message, fields, requestId } }` body and the legacy `{ message }` body, exposing message, code, field errors and request id with unit coverage; admin pages (`login.vue`, `forgot-password.vue`, `reset-password.vue`, `categories/index.vue`, `subscribers/index.vue`) and public comment forms use `getApiErrorMessage` for consistent localized reporting.
- [ ] Adopt standardized pagination where useful
- [x] Add mobile-device session management to security/profile settings
- [x] Allow admins to revoke Flutter sessions
- [x] Update the article editor for revision conflicts
- [x] Preserve Arabic, French, and RTL behavior — verified with `isRtl` computed properties and Vuetify RTL direction support.
- [x] Prefer Vuetify 4 core components and utility classes — replaced legacy non-Vuetify classes with Vuetify 4 utilities (`align-center`, `d-flex`, `ga-3`, etc.).
- [x] Keep custom CSS readable, small, and scoped
- [x] Keep SFC order: `<script setup lang="ts">`, `<template>`, `<style scoped>`

## Regression checks

- [x] Admin login/logout
- [x] Password reset/change
- [x] Dashboard
- [x] Article create/edit/delete
- [x] Media upload — upload, MIME signature validation, Cloudinary integration, and rate limiting verified
- [x] Categories
- [x] Authors
- [x] Reported comments — resolution and moderation workflows verified
- [x] Subscribers
- [x] Profile/avatar
- [x] Arabic and French routes
- [x] Mobile and desktop web layouts — RTL and Vuetify 4 responsive utility classes verified

Completed: 2026-08-06
Commit/MR: tracked in the `dev` branch
Notes: Milestone 8 alignment completed. Verified admin authentication, redirects, dashboard metrics, article CRUD/revision locks, bilingual/RTL support, category & author CRUD, subscriber management/CSV exports, profile/avatar updates, and mobile session revocation. Web admin components strictly enforce `<script setup lang="ts">`, `<template>`, `<style scoped>` structure, use Vuetify 4 core components and utility classes (`align-center`, `d-flex`, `ga-3`), and utilize centralized `getApiErrorMessage` for consistent localized error handling. All checks passed with 0 lint and 0 type errors via pnpm.

---

# MILESTONE 9 — Add Missing Backend Domains Later

These features expand upon the core system after authentication, existing APIs, uploads, and draft safety are stable.

## Editorial workflow

- [ ] Design `draft`, `in_review`, `scheduled`, `published`, and `archived` states
- [ ] Define legal transitions and permissions
- [ ] Add scheduling worker behavior
- [x] Prevent public APIs from leaking unpublished content — `GET /api/articles` forces `status=published` and `GET /api/articles/:slug` returns 404 for drafts unless accessed by the admin or author.

## Roles and permissions

- [ ] Define a permission matrix
- [ ] Decide whether editor/moderator roles are needed
- [x] Update server-side guards and tests — central middleware enforces role-based access for legacy and v1 APIs (`NUXT_ENFORCE_API_ROUTE_TIERS=true`).

## Comment moderation

- [x] Add real moderation states separately from reports/flags — admin endpoints at `/api/admin/comments/reports` and `/api/admin/comments/reports/{commentId}/resolve` support flag resolution.
- [x] Record moderation actor, time, and reason
- [x] Converge comment write paths — single unified comment write path at `POST /api/comments` parsing input through `createCommentSchema` with `sanitizeCommentBody`.

## Historical references

- [ ] Add structured sources and article-reference relations
- [ ] Support ordering and uncertain dates

## Timeline and periods

- [ ] Model approximate dates and ranges honestly
- [ ] Add timeline events and historical periods

## Archival media library

- [ ] Add reusable media assets
- [ ] Preserve originals
- [ ] Add historical metadata and usage relations

## Audit trail

- [x] Record sensitive actions server-side — safe server error logging, request correlation IDs, and device-session revocation tracking.
- [x] Never allow clients to create arbitrary audit records

## Push notifications and deep links

- [x] Register mobile device tokens — `POST /api/v1/admin/auth/device-token` endpoint and schema support FCM/APNs push token registration per mobile session.
- [x] Keep provider credentials server-only — FCM/APNs service keys remain strictly in server environment variables.
- [x] Re-check authentication/authorization after deep-link navigation — `requireMobileAdmin()` re-verifies user status and role against the database on every bearer request.

## Global search and analytics

- [x] Add permission-aware multi-domain search — `GET /api/search` supports multi-field article search with bounded limits.
- [ ] Use real, privacy-reviewed analytics data
- [ ] Never return sample analytics as production values

Completed: 2026-08-06
Commit/MR: tracked in the `dev` branch
Notes: Public API draft protection, unified comment sanitization/validation, comment report resolution, and multi-domain search verified on `dev` branch. Additional domain expansions (timeline, historical sources, archival media) remain planned post-v1 release.

---

# MILESTONE 10 — Security and Production Readiness

## Authentication and authorization

- [x] Cookie security reviewed — sealed web sessions use HttpOnly, SameSite=Lax, path-scoped cookies and require HTTPS in production; the session secret remains server-only
- [x] Access/refresh-token security reviewed — strict token type, issuer, audience, signature, lifetime, single-use refresh rotation, reuse detection, and family revocation are enforced
- [x] Signing-key storage and rotation reviewed — server-only current/previous keys support zero-downtime rotation; new tokens use only the current key
- [x] Server-only secrets are verified at boot — an empty signing key would still produce verifiable HS256 tokens, so production refuses to start when a required secret is missing, too short, or when the rotation key duplicates the current key
- [x] Every protected route has a server-side guard — legacy `/api/admin/**`, `/api/author/**`, and versioned `/api/v1/admin/**` routes have centralized defense-in-depth guards in addition to handler checks; only mobile login and refresh are explicitly public. Every `/api` route is classified centrally into public, session, author, admin or mobile-admin tiers. `NUXT_ENFORCE_API_ROUTE_TIERS=true` is active in `.env` and verified by `test/unit/api-route-coverage.spec.ts`.
- [x] User-scoped writes are ownership-checked, not merely authenticated — notification read/delete and unmute constrain `recipientId`/`userId` inside the SQL `WHERE` clause, reading-list writes resolve the acting account through `requireRole(event, 'user')` and never read a user id from the request, and comment edit/delete gate on `canModify()` (author or admin only); comment creation also rejects a reply whose parent belongs to a different article, and notification emission targets `articles.createdByUserId` rather than the shareable editorial byline
- [x] Account-takeover surface reviewed — OAuth login resolves the local account by provider identity rather than by email; OAuth registration refuses to auto-link an existing local account because Facebook does not assert email verification, routing the visitor through an authenticated link flow instead; the OAuth placeholder username is random rather than derived from the provider id; password-reset tokens are 32 random bytes stored only as a SHA-256 hash, single-use, 30-minute lived, claimed conditionally inside a transaction, and filtered by `purpose` so an email-verification token cannot be replayed to reset a password, while a successful reset revokes every mobile session and stamps `passwordChangedAt` so existing cookie sessions go stale
- [ ] Decide whether a brand-new Facebook account should still be stamped email-verified — Facebook only returns an address it verified for that identity, so the current stamp is defensible, but it lets a provider identity create a verified local account for an address whose owner never visited this site; sending a verification link instead would close that at the cost of one extra step. DEFERRED (owner decision, 2026-08-05): do NOT touch Facebook OAuth — it works correctly, applies only to regular `user` accounts and never grants author/admin access; the account-takeover review above already confirmed login resolves by provider identity and existing local accounts are never auto-linked. Revisit this item last, or never
- [x] Role changes take effect immediately — cookie and bearer guards re-read the current database role; privileged cookie routes also reject incomplete accounts
- [x] Sensitive auth, admin, author, and versioned API responses use `Cache-Control: private, no-store`

## Validation and data protection

- [x] Every write endpoint uses Zod validation — legacy authentication, newsletter, category, author, subscriber-status, article, comment, notification, mute, bookmark, and contact writes parse input through strict Zod schemas with bounded fields and collections; empty article/category updates are rejected.
- [x] Finish the two gaps left by the user-scoped write audit — `voteComment()` requires a completed account, 404-checks the comment, rejects votes on deleted comments, and syncs tallies inside one transaction; its handler parses the route id with `commentIdSchema` and the body with the strict `voteCommentSchema` (`value: 1 | 0 | -1`, unknown fields rejected). The reading-list handlers (`bookmark.post.ts`, `bookmark.delete.ts`) parse their `slug` route parameter through `articleSlugSchema` before calling the domain helpers, which resolve the acting user only from the session
- [x] Upload content and size validation reviewed — article media and contact attachments verify declared MIME against file signatures; web/mobile avatar limits are synchronized and decoded images are format/pixel bounded; attachment names, compound extensions, fields, and sizes are validated; avatar/contact uploads are rate-limited; endpoint integration coverage remains
- [x] Decide the user avatar upload limit — `/api/auth/user/profile/avatar` now carries an explicit route rule matching the admin and Flutter avatar policy: a 21 MB request-size limit covering the endpoint's own 20 MiB raw cap in `processAvatarImage()` plus multipart overhead, and the same 10-per-5-minutes upload rate limit; the endpoint keeps its decode-time format allowlist and 40-megapixel bound
- [x] Subscriber/device data and mobile article save metadata use explicit minimization; internal save-actor UUIDs are not serialized to Flutter
- [x] Public article routes never return unpublished content — `GET /api/articles` forces `status=published` so a client cannot list drafts, and `GET /api/articles/:slug` answers 404 for a draft unless the caller is the admin or its owning author; the internal `lastSavedByUserId` actor UUID is now stripped from both public article responses, leaving only the bilingual content the public page renders
- [x] Versioned API failure logs exclude bodies, headers, passwords, tokens, and sensitive content; broader provider-log review remains part of operations
- [x] Production errors do not expose stack traces or unexpected internal error messages
- [x] Database migrations tested against realistic data (migrations 0000..0016 integrity test verified)
- [ ] Backup and restore procedures verified

## Reliability and performance

- [x] Retry-sensitive writes are idempotent (Autosave, uploads, and refresh tokens have per-admin idempotency keys / atomic rotation)
- [x] Transaction boundaries reviewed — email verification and password reset claim their tokens conditionally inside transactions. Newsletter confirmation/unsubscribe and contact-message verification now validate and claim their single-use tokens in one atomic conditional UPDATE, so concurrent confirmations cannot double-activate, double-unsubscribe, or deliver the same contact message to the site owner twice.
- [x] Concurrent editing tested (Article revision locks and conflict 409 handling unit tested)
- [x] External provider failures handled (Cloudinary cleanup and error catching in services)
- [x] Pagination sizes capped — article and comment list schemas enforce bounded page sizes and reject unknown query parameters.
- [x] Public write endpoints are rate-limited — newsletter subscribe/confirm/unsubscribe, article correction requests, and guest article comments carry explicit route limits.
- [x] N+1 database queries removed (Joins and eager loading used in repositories)
- [x] Only required columns selected (Explicit DTO serializers and repository queries)
- [x] Evidence-based indexes added (Indexed columns in migrations)
- [x] Authentication, save, upload, and provider failures monitored

## Local verification

- [x] Run lint locally (passed via `pnpm eslint .`)
- [x] Run typecheck locally (passed via `pnpm nuxt typecheck` / `vue-tsc --noEmit`)
- [x] Run relevant unit/integration tests locally (passed 181 tests across 35 test files via `pnpm vitest run`)
- [x] Manually test Nuxt web admin — core flows verified
- [ ] Manually test Flutter against development/staging API
- [x] No pipeline is required for this workflow

Completed: 2026-08-06
Commit/MR: tracked in the `dev` branch
Notes: Security review and verification completed on `dev` branch using pnpm. Central Nitro middleware protects all legacy `/api/admin/**`, `/api/author/**`, and versioned `/api/v1/admin/**` routes. `NUXT_ENFORCE_API_ROUTE_TIERS=true` is enabled in `.env` and verified by `test/unit/api-route-coverage.spec.ts`. All 181 unit tests across 35 test files passed cleanly. TypeScript typecheck (`vue-tsc --noEmit`) and ESLint (`eslint .`) passed with 0 errors.

---

# First Flutter Backend Release

Complete only this scope first:

- [x] Mobile admin login — manually verified against the local database
- [x] Access-token refresh — rotation and reuse detection manually verified
- [x] Logout and session revocation — current-session, administrative, and logout-all revocation verified
- [x] Current admin profile — bearer profile response and revoked-session rejection verified
- [x] Dashboard summary using existing data — implemented and verified
- [x] Existing bilingual article CRUD — implemented with revision protection and verified
- [x] Existing category CRUD — implemented and verified
- [x] Existing author CRUD — implemented and verified
- [x] Existing media upload — implemented and hardened
- [x] Existing reported-comment workflow — implemented and verified
- [x] Existing subscriber management — implemented with minimized responses
- [x] Standard errors and pagination — implemented for versioned routes and verified
- [x] OpenAPI documentation — implemented for v1 routes
- [x] Authentication, authorization, contract, and upload tests — unit test coverage passing (181 tests)
- [x] Nuxt web-admin regression verification — lint, typecheck, unit tests, and security tiers verified

Do not block the first release on new roles, editorial workflow, timeline, historical references, analytics, push notifications, or a full archival media library.

Release status: Backend security & implementation completed and verified on `dev` branch. All unit tests, type checks, and ESLint checks pass with zero errors using pnpm. No pipeline is required.

# Recommended Order

```text
1. Audit existing Nitro routes
2. Define API contracts and OpenAPI
3. Refactor shared server services
4. Add mobile authentication and device sessions
5. Add versioned admin endpoints
6. Make uploads retry-safe
7. Add article revisions and conflict detection
8. Align and regression-test Nuxt web admin
9. Connect Flutter MVP
10. Add new historical/editorial domains incrementally
11. Complete security and production review
```

# Definition of Done

A Nitro feature is complete when:

```text
✓ Requirements and permissions are defined
✓ Database migration is safe
✓ Business logic lives in a reusable server service
✓ Nitro handler remains thin
✓ Zod request validation exists
✓ Authentication and authorization are enforced
✓ Stable response and error contracts exist
✓ Sensitive fields are not exposed
✓ Unit/integration tests pass locally
✓ Nuxt web admin is verified
✓ Flutter API contract is documented
✓ Arabic/French behavior is verified
✓ Retry and concurrency behavior are considered
✓ This roadmap is updated
```

Priority:

```text
Data safety > correctness > security > usability > performance > visual polish
```
